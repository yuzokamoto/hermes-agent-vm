# Runtime data directory. Keep this outside the repository checkout.
HERMES_HOME=$HOME/.hermes

# Optional installer override for controlled mirrors or testing.
HERMES_INSTALL_URL=https://hermes-agent.nousresearch.com/install.sh

# Provider credentials are intentionally omitted.
# Configure them through `hermes setup` or your cloud secret manager.
